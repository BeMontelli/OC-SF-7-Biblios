{% extends 'base.html.twig' %}

{% block title %}Editor{% endblock %}

{% block body %}
<div class="container mt-5 mb-4 pt-4">
    <h1>Editor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ editor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ editor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_editor_index') }}">back to list</a>

    <a href="{{ path('app_editor_edit', {'id': editor.id}) }}">edit</a>

    {{ include('admin/editor/_delete_form.html.twig') }}
</div>
{% endblock %}
